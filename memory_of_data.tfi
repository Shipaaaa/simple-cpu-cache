


// Instantiate the module
memory_of_data instance_name (
    .clk(clk), 
    .not_reset(not_reset), 
    .index(index), 
    .channel(channel), 
    .need_write_data(need_write_data), 
    .data_in(data_in), 
    .data_out(data_out)
    );


